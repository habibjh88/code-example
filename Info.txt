//get IP from cmd
ipconfig


Chrome tab reload automatically fixed
chrome://settings/performance 


//Responsive font-size || width/height
height: clamp(460px, 40vw, 740px);
clamp(minVal, 100vw-40vw = 60vw, maxVal)
That means the clamp function works when the window resizes between 100vw to 60vw 


//Remove all .DS_Store file from projec. Just run the below command in the terminal

find . -name .DS_Store -print0 | xargs -0 git rm -f --ignore-unmatch